version: "2"
services:
  node:
    image: "arm32v7/node:8"
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=dev
    volumes:
      - ./:/home/node/app
    expose:
      - "8080"
    command: "npm start"